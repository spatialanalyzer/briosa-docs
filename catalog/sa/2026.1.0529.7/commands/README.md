# SA 2026.1.0529.7 command records

Issue #16 will publish the initial 1,412-record baseline in this directory.
Each JSON file will contain one command, and its path without `.json` will
equal the record's stable `slug`.

The target manifest intentionally permits incomplete coverage during v0.x
development. Complete coverage is required before the target can enter the
`v1_release` publication phase.
